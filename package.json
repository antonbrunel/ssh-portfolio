{
  "name": "ssh-portfolio",
  "version": "1.0.0",
  "description": "Interactive SSH portfolio — Anton Brunel",
  "main": "dist/server.js",
  "scripts": {
    "build": "tsc",
    "start": "node dist/server.js",
    "dev": "ts-node src/server.ts",
    "generate-keys": "ts-node scripts/generate-keys.ts"
  },
  "dependencies": {
    "ssh2": "^1.15.0"
  },
  "devDependencies": {
    "@types/node": "^20.0.0",
    "@types/ssh2": "^1.15.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.4.0"
  }
}
